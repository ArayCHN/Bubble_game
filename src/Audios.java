package bubblegame;
import sun.audio.*;

public class Audios {
    AudioStream audioMenu, audioBgm, audioTick, audioLaunch;
    public Audios() {
        // try {
        //     audioMenu = new AudioStream(BubbleGame.class.getResourceAsStream("../sound/menu.wav"));
        //     audioBgm = new AudioStream(BubbleGame.class.getResourceAsStream("../sound/bgm.wav"));
        //     audioTick = new AudioStream(BubbleGame.class.getResourceAsStream("../sound/tick.wav"));
        //     audioLaunch = new AudioStream(BubbleGame.class.getResourceAsStream("../sound/launch.wav"));
        // } catch (Exception e) {
        //     e.printStackTrace();
        // }
    }
}